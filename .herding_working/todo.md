注意：所有的文件应该一步一步的从框架开始完成，而不是一次写完。
注意：所有的交互ui都应该使用tauri实现。而不是依赖系统
注意：此mcp所有的console输出都是针对cursor的。必须严肃、简洁。禁止emoji


## plan计划补充
同时以下内容加入到后续的带实现中

在plan计划的提示词中应该加入：你需要分析用户想要达到的目标，并以此为目标拆分任务。
在plan计划中应该存储此次计划的目标。并在每次调用完成单个任务之后，还有后续任务时，额外强调此目标
 
 

 
 我还需要你在调用plan之后，弹出窗口让用户对所有创建的计划进行审查。用户可以对计划进行拖动排序（可ctrl+z撤销）、内容编辑、删除（可ctrl+z撤销）等，也可以让用户输入文字部分，让cursor重新制订计划。因此需要有一个计划的session（id/或者是此计划的文件名）需要cursor传入。

在用户编辑完之后将结果以及新任务的id告知给cursor并让cursor开始执行。

在完成todo时，由于牧羊犬不知道是哪个任务中的item，因此约定一个规则：根据时间从新到旧依次找对应的任务（按照任务名称的汉明距离排序取最接近的）。实在找不到时，提示cursor几个汉明距离最接近的item


## app需求补充

增对tauri创建的app由于实时编译成本过高，因此需要提前使用github action编译不同平台的对应app。并存放在release中。因此是需要写对应的action

在安装时，需要根据所属平台下载对应不同的对应版本的app到安装目录。然后在代码中进行调用